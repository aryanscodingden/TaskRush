export const metadata = {
    title: "TaskRush",
    description: "Focus better, now with a good UI.",
};

export default function Home() {
    return (
        <main className="w-screen min-h-screen bg-neutral-100">
        </main>
    );
}